# WhatsApp Web Interface Environment Variables
PORT=3000
NODE_ENV=production
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true
PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium

# Optional: Custom backend URL for production
# BACKEND_URL=your-domain.com

# Optional: Database configuration (if needed later)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=whatsapp_web
# DB_USER=username
# DB_PASSWORD=password

# Optional: Redis configuration (if needed later)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=

# Optional: JWT secret for authentication (if needed later)
# JWT_SECRET=your-secret-key-here
